language: python
python:
        - 3.6
before_install:
        - sudo apt-get update
        - sudo apt-get -y install bedtools
        - python --version
        - pip install -U pip
        - pip install -U pytest
        - pip install codecov
install:
        - pip install ".[test]" .
script: pytest
after_success:
        - codecov

